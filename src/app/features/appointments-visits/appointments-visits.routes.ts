import { Routes } from '@angular/router';
import { AppointmentsVisitsComponent } from './appointments-visits.component';

export const routesAppointmentsVisits: Routes = [
  {
    path: '',
    component: AppointmentsVisitsComponent,
  }
];